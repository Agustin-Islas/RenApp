name: frontend_dialysis_record
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Authentication
  supabase_flutter: ^2.6.0

  # Networking & Storage
  dio: ^5.9.0
  universal_html: ^2.2.4
  flutter_secure_storage: ^9.2.2

  # Utils
  intl: ^0.20.2
  pdf: ^3.11.3
  share_plus: ^10.1.4

  # State management
  flutter_riverpod: ^2.6.1

  # Navigation
  go_router: ^14.8.1

  # Design system
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  phosphoricons_flutter: any
  skeletonizer: ^2.1.3
  responsive_framework: ^1.5.1

  # Animations (assets pending)
  rive: ^0.14.7
  lottie: ^3.3.1

  # Charts
  fl_chart: ^0.70.2
  syncfusion_flutter_charts: ^33.2.15
  scrollable_positioned_list: ^0.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/new_icon_renapp_squared.jpg"
  min_sdk_android: 21

flutter:
  uses-material-design: true
  assets:
    - assets/images/
